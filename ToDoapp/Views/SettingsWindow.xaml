<Window x:Class="ToDoapp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoapp.Views"
        xmlns:vm="clr-namespace:ToDoapp.ViewModels"
        xmlns:models="clr-namespace:ToDoapp.Models"
        mc:Ignorable="d"
        Title="设置"
        Height="500" Width="700"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FontSize="14">
    
    <Window.Resources>
        <Style x:Key="SettingItemButtonStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#202020" CornerRadius="8" BorderBrush="#3D3D3D" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="12" Opacity="0.4" Color="#000000"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#1F1F1F" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="设置" 
                              Foreground="{StaticResource TextPrimaryBrush}" 
                              FontSize="16" FontWeight="SemiBold" 
                              VerticalAlignment="Center"/>
                    
                    <Button Grid.Column="1"
                           Width="32" Height="32" 
                           Click="CloseButton_Click" 
                           Style="{StaticResource IconButtonStyle}" 
                           Foreground="{StaticResource DangerBrush}">
                        <Viewbox Width="{StaticResource DialogTitleBarIconSize}" Height="{StaticResource DialogTitleBarIconSize}">
                            <Path Data="{StaticResource CloseIconGeometry}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform"/>
                        </Viewbox>
                    </Button>
                </Grid>
            </Border>
            
            <!-- 主内容区域 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左侧设置项列表 -->
                <Border Grid.Column="0" Background="#282828" 
                        BorderBrush="#3D3D3D" BorderThickness="0,0,1,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="SettingItemsPanel" Margin="8,12">
                            
                            <!-- 常规设置分类 -->
                            <TextBlock Text="常规" 
                                      Foreground="{StaticResource TextMutedBrush}" 
                                      FontSize="11" FontWeight="SemiBold" 
                                      Margin="12,8,12,6"
                                      x:Name="GeneralCategoryHeader"/>
                            
                            <ListBox x:Name="GeneralSettingsList"
                                     Background="Transparent" 
                                     BorderThickness="0"
                                     ItemContainerStyle="{StaticResource SettingItemButtonStyle}"
                                     SelectionChanged="SettingItem_SelectionChanged"
                                     SelectionMode="Single">
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:SettingItem}">
                                        <StackPanel>
                                            <TextBlock x:Name="NameText" Text="{Binding Name}" 
                                                      FontSize="13" 
                                                      FontWeight="Medium"/>
                                            <TextBlock x:Name="DescriptionText" Text="{Binding Description}" 
                                                      FontSize="11" 
                                                      Foreground="{StaticResource TextMutedBrush}"
                                                      TextTrimming="CharacterEllipsis"
                                                      MaxWidth="160"/>
                                        </StackPanel>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                <Setter TargetName="DescriptionText" Property="Foreground" Value="#E5E7EB"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            
                            <!-- 外观设置分类 -->
                            <TextBlock Text="外观" 
                                      Foreground="{StaticResource TextMutedBrush}" 
                                      FontSize="11" FontWeight="SemiBold" 
                                      Margin="12,16,12,6"/>
                            
                            <ListBox x:Name="AppearanceSettingsList"
                                     Background="Transparent" 
                                     BorderThickness="0"
                                     ItemContainerStyle="{StaticResource SettingItemButtonStyle}"
                                     SelectionChanged="SettingItem_SelectionChanged"
                                     SelectionMode="Single">
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:SettingItem}">
                                        <StackPanel>
                                            <TextBlock x:Name="NameText" Text="{Binding Name}" 
                                                      FontSize="13" 
                                                      FontWeight="Medium"/>
                                            <TextBlock x:Name="DescriptionText" Text="{Binding Description}" 
                                                      FontSize="11" 
                                                      Foreground="{StaticResource TextMutedBrush}"
                                                      TextTrimming="CharacterEllipsis"
                                                      MaxWidth="160"/>
                                        </StackPanel>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                <Setter TargetName="DescriptionText" Property="Foreground" Value="#E5E7EB"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            
                            
                        </StackPanel>
                    </ScrollViewer>
                </Border>
                
                <!-- 右侧设置内容区域 -->
                <Border Grid.Column="1" Background="#202020">
                    <ContentControl x:Name="ContentArea" 
                                   Content="{Binding CurrentContent}"
                                   Margin="0"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
