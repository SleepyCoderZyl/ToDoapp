<Window x:Class="ToDoapp.Views.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoapp.Views"
        xmlns:app="clr-namespace:ToDoapp"
        mc:Ignorable="d"
        Title="待办便签" Height="360" Width="280"
        MinHeight="200" MinWidth="200"
        MaxHeight="450" MaxWidth="350"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        FontSize="14"
        WindowStartupLocation="Manual">

    <Window.ContextMenu>
        <ContextMenu Style="{StaticResource ModernContextMenuStyle}">
            <MenuItem Header="切换到主页面" Click="ToggleMainWindow_Click" Style="{StaticResource ModernMenuItemStyle}"/>
            <MenuItem Header="进入沉浸模式" Name="ImmersionModeMenuItem" Click="ToggleImmersionMode_Click" Style="{StaticResource ModernMenuItemStyle}"/>
            <Separator Style="{StaticResource ModernSeparatorStyle}"/>
            <MenuItem Header="设置" Click="ShowSettings_Click" Style="{StaticResource ModernMenuItemStyle}"/>
            <MenuItem Header="退出程序" Click="ExitApplication_Click" Style="{StaticResource ModernMenuItemStyle}"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Window.Resources>
        <app:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Width="6">
                            <Track x:Name="PART_Track" Orientation="Vertical" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#3D3D3D" CornerRadius="3" Margin="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- 背景层 - 单独控制透明度，不影响内容 -->
        <Border x:Name="BackgroundBorder"
                Background="#202020"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="#3D3D3D">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.3" Color="#000000"/>
            </Border.Effect>
        </Border>

        <!-- 内容层 - 独立于背景层，不受背景透明度影响 -->
        <Grid Margin="6">
            <ListBox x:Name="WidgetTasksListBox"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Visible"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" Background="Transparent"
                                            CornerRadius="6" Margin="0,2" Padding="6,4">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#2D2D2D"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid MinHeight="40">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}"
                                     Margin="0,0,8,0" VerticalAlignment="Center"
                                     Checked="TaskCheckBox_Checked" Unchecked="TaskCheckBox_Checked"
                                     Style="{StaticResource ModernCheckBoxStyle}"/>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}"
                                          TextTrimming="CharacterEllipsis"
                                          FontSize="13"
                                          Margin="0,0,4,0"
                                          Foreground="{StaticResource TextPrimaryBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding DueDateDisplay}"
                                          FontSize="11"
                                          Margin="0,3,0,0"
                                          Visibility="{Binding DueDateDisplay, Converter={StaticResource StringToVisibilityConverter}}"
                                          Foreground="{StaticResource TextSecondaryBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock x:Name="EmptyMessage"
                      Text="暂无待办事项"
                      Foreground="{StaticResource TextMutedBrush}"
                      FontSize="14"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
