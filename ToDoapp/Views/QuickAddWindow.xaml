<Window x:Class="ToDoapp.Views.QuickAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoapp.Views"
        mc:Ignorable="d"
        Title="快速添加待办"
        SizeToContent="Height"
        Width="600"
        MinHeight="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        FontSize="14">
    
    <Border Background="{DynamicResource DialogBackgroundBrush}" CornerRadius="8" BorderBrush="{DynamicResource DialogBorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="4" BlurRadius="20" Opacity="0.5" Color="#000000"/>
        </Border.Effect>
        
        <Grid Margin="20" FocusManager.FocusedElement="{Binding ElementName=InputTextBox}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="快速添加待办" 
                      Foreground="{DynamicResource TextPrimaryBrush}" 
                      FontSize="18" FontWeight="SemiBold" 
                      Margin="0,0,0,16"
                      MouseLeftButtonDown="TitleText_MouseLeftButtonDown"
                      Cursor="SizeAll"/>
            
            <TextBox Grid.Row="1" x:Name="InputTextBox"
                    Style="{StaticResource ModernTextBoxStyle}"
                    FontSize="15"
                    Padding="14,12"
                    MaxHeight="60"
                    MaxLength="200"
                    TextWrapping="Wrap"
                    AcceptsReturn="False"
                    TextChanged="InputTextBox_TextChanged"
                    KeyDown="InputTextBox_KeyDown"
                    Margin="0,0,0,12"/>
            
            <Border Grid.Row="2" Background="{DynamicResource PanelBrush}" CornerRadius="8" Padding="14,12" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="解析预览：" 
                              Foreground="{DynamicResource TextMutedBrush}" 
                              FontSize="12" FontWeight="Medium"
                              VerticalAlignment="Top"/>
                    
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock x:Name="PreviewTitleText" 
                                  Text="等待输入..." 
                                  Foreground="{DynamicResource TextPrimaryBrush}" 
                                  FontSize="14" FontWeight="Medium"
                                  TextWrapping="Wrap"/>
                        <TextBlock x:Name="PreviewDateText" 
                                  Text="无" 
                                  Foreground="{DynamicResource TextSecondaryBrush}" 
                                  FontSize="12"
                                  TextWrapping="Wrap"
                                  Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          Text="按 Enter 确认，ESC 取消" 
                          Foreground="{DynamicResource TextMutedBrush}" 
                          FontSize="12"
                          VerticalAlignment="Center"/>
                
                <Button Grid.Column="1" Content="取消"
                       Style="{StaticResource SecondaryButtonStyle}"
                       Margin="0,0,10,0"
                       Click="CloseButton_Click"/>
                
                <Button Grid.Column="2" Content="添加"
                       Style="{StaticResource ModernButtonStyle}"
                       Click="AddButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>

