<UserControl x:Class="ToDoapp.Views.Settings.Appearance.AlwaysOnTopSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AlwaysOnTopSettingsViewModel}"
             xmlns:vm="clr-namespace:ToDoapp.ViewModels.Settings.Appearance"
             FontSize="14">
    <StackPanel Margin="20">
        <TextBlock Text="小组件置顶"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,20"/>
        <TextBlock Text="启用后，小组件将始终显示在所有其他窗口的最上层。"
                   FontSize="13"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <CheckBox IsChecked="{Binding IsEnabled}"
                      Style="{StaticResource ModernCheckBoxStyle}"/>
            <TextBlock Text="{Binding StatusText}"
                       FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</UserControl>
