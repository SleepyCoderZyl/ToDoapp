<UserControl x:Class="ToDoapp.Views.Settings.General.StartInWidgetModeSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:StartInWidgetModeSettingsViewModel}"
             xmlns:vm="clr-namespace:ToDoapp.ViewModels.Settings.General"
             FontSize="14">
    <StackPanel Margin="20">
        <TextBlock Text="启动模式"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,20"/>
        <TextBlock Text="启用后，程序启动时将自动进入小组件模式，不显示主页面。"
                   FontSize="13"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <CheckBox IsChecked="{Binding StartInWidgetMode}"
                      Style="{StaticResource ModernCheckBoxStyle}"/>
            <TextBlock Text="{Binding StatusText}"
                       FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</UserControl>
