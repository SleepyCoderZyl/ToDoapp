<UserControl x:Class="ToDoapp.Views.Settings.General.HotKeySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=settings:HotKeySettingsViewModel}"
             xmlns:settings="clr-namespace:ToDoapp.ViewModels.Settings.General"
             FontSize="14">
    <StackPanel Margin="20">
        <TextBlock Text="全局快捷键"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,12"/>
        <TextBlock Text="设置快速添加待办、显示主页和隐藏小组件的全局快捷键。"
                   FontSize="13"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <ItemsControl ItemsSource="{Binding Entries}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type settings:HotKeyEntryViewModel}">
                    <Border Loaded="HotKeyRow_Loaded"
                            Unloaded="HotKeyRow_Unloaded"
                            Tag="{Binding}"
                            Padding="0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding Title}"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,16,0"/>

                            <CheckBox Grid.Column="1"
                                      IsChecked="{Binding IsEnabled}"
                                      Style="{StaticResource ModernCheckBoxStyle}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,16,0"
                                      ToolTip="启用或禁用此快捷键"/>

                            <TextBox x:Name="HotKeyInput"
                                     Grid.Column="2"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Width="220"
                                     IsReadOnly="True"
                                     IsEnabled="{Binding IsControlsEnabled}"
                                     Foreground="{DynamicResource PrimaryBrush}"
                                     VerticalAlignment="Center"
                                     Cursor="Hand"
                                     Margin="0,0,10,0"/>

                            <Button Grid.Column="3"
                                    Content="重置"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    VerticalAlignment="Center"
                                    IsEnabled="{Binding IsControlsEnabled}"
                                    Command="{Binding ResetHotKeyCommand}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
