<UserControl x:Class="ToDoapp.Views.Settings.General.StartupSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:StartupSettingsViewModel}"
             xmlns:vm="clr-namespace:ToDoapp.ViewModels.Settings.General"
             FontSize="14">
    <StackPanel Margin="20">
        <TextBlock Text="开机自启动"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,20"/>
        <TextBlock Text="启用后，应用程序将在 Windows 启动时自动运行。"
                   FontSize="13"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <CheckBox IsChecked="{Binding IsEnabled}"
                      Style="{StaticResource ModernCheckBoxStyle}"/>
            <TextBlock Text="{Binding StatusText}"
                       FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</UserControl>
